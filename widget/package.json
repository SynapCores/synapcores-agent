{
  "name": "@synapcores/widget",
  "version": "0.3.0-identity",
  "description": "Drop-in chat widget powered by SynapCores. Sprint 3 — identify() + persistent conversation across page loads.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "main": "dist/widget.js",
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "node build.mjs",
    "build:watch": "node build.mjs --watch",
    "dev": "node build.mjs --watch & python3 -m http.server 5050"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  }
}
